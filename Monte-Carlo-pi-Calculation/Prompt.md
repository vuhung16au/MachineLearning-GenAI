Base on README.md, create a web application to demonstrate using animation how to use Monte Carlog method to calculate value of pi.

Your outputs are:

- index.html: Only juse HTML, CSS and Javascript
- monte-carlo.css
- monte-carlo.js

The simulation/animation runs automatically when user loads index.html and stops when reaching error less than 0.001

- Show the number of iterations below "Error" and above "Actual π"
- Add the mathematical explanation of Monte Carlo method at the end of this page. 
- Show elapsed time of the program. How fast the estimated pi is converged to actual \pi?
- The simulation stops when the error is less than 0.1
- Show the estimated pi and actual pi in different colors
- Show the error in different color
- Generate N=1 random numbers at a time
- Show the coordinates of the random points a boxed area